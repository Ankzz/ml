
Linear Transformation 3 criteria:

- f(0) = T(0) = 0
- f(x+y) = f(x) + f(y) = T(x) + T(y)
- f(cx) = cf(x) = cT(x)

Q1. Find which one of the following is a valid linear transformation  

1. T: R^2 -> R^3, f(x1, x2) = (x, x+y, x+1)
   Soln:  
   f(0,0) = (0, 0, 1)  
   Hence, f(x1, x2) is not a valid transformation

2. T: R^2 -> R^2, f(x1, x2) = (xy, x+y)  
   Soln:  
   f(0,0) = (0, 0)

        f(a1,a2) = (a1a2, a1+a2)
        f(b1,b2) = (b1b2, a1+a2)

        f(a1+b1, a2+b2) = ( (a1+b1*)*(a2+b2), (a1+b1)+(a2+b2))  
        = ((a1a2+a1b2+b1a2+b1b2)), (a1+a1)+(b1+b2))
        = ((a1a2+b1b2)+(b1a2+a1b2)), (a1+a2)+(b1+b2))
        = ((a1a2+b1b2), (a1+a2)+(b1+b2))+((b1a2+a1b2), 0)

    As we have an extra value of (a1b2+b1a2) in first element and cannot be split to f(a1,a2) + f(b1, b2).

3. T: R^2 -> R^3, f(x1, x2) = (x+2y, x+y+1, 2x-y)  
   Soln:
   f(0,0) = (0,1,0)  
   As first rule fails, so f(x1,x2) is invalid


Q 2: Find the transformation matrix T: U -> V = T: R^2 -> R^2  
f(x1, x2) = | x1 - x2   |  
            | 2x1 + 3x2 |

1. Use the standard basis for both vector space
2. Use the standard basis for U and {[1,1], [-1,1]} for V
3. Use {[1,1],[-1,1]} for U and {[1,1],[-1,2]} for V

Soln:
1. f(1,0) =  


Q 3. --- To fill ----

Four fundamental subspaces:

1. Column space is C(A) a subspace of R<sup>m</sup>
2. Row space is C(A<sup>T</sup>), a subspace of R<sup>n</sup>
3. Nullspace is N(A), s subspace of R<sup>n</sup>
4. Left Nullspace is N(A<sup>T</sup>), a subsapce of R<sup>m</sup>

Q4. Obtain four fundamental spaces of A and find its rank and nullity

        2 4 6 8  10  
    A = 2 0 2 4  -2  
        2 2 4 0   2
        4 4 8 12  8
